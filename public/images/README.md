# Product image assets

Put only verified, real product assets here before launch:

- `hero-back-support.jpg`
- `support-detail.jpg`
- `size-guide.jpg`

Then replace the matching placeholders in `components/SalesPage.tsx` with Next/Image or standard `<img>` elements.
